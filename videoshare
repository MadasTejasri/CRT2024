#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  int p;
   scanf("%d",&p);
   if(p>=30&&p<=100)
    {   
     if(p>=30&&p<=50)
     {
        printf("Average");
     }
     else if(p>=51&&p<=60)
      {
        printf("Good");
      }
     else if(p>=61&&p<=100)
      {
        printf("Excellent");
      }
    else
    {
        printf("Outstanding");
    }
   }
   else
   {
       printf("Invalid Input");
   }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
