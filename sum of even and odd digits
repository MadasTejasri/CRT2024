#include <stdio.h>
#include<math.h>

int main() {
    // Write C code here
    int n,r,s1=0,s2=0;
    scanf("%d",&n);
    while(n>0)
    {
    r=n%10;
    n=n/10;
    if(r%2==0)
    s1=s1+r;
    else
    s2=s2+r;
    }
        printf("%d\n%d",s1,s2);
    return 0;
}
