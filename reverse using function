#include <stdio.h>
 int reverse ()
    {
        int a,r=0,d;
    scanf("%d",&a);
    while(a>0)
    {
     d=a%10;
     a=a/10;
     r=r*10+d;
    }
     printf("%d",r);
    }
     int main() {
    // Write C code here
    reverse();
}
