#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int a[n],i;
    for(i=0;i<n;i++)
    {
       scanf("%d",&a[i]);
    }
    int p;
    scanf("%d",&p);
    if(p<=n)
    {
      for(i=0;i<n;i++)
      {   
        if(i==(p-1))
        {
            //printf("");
        }
          else{
        printf("%d\n",a[i]);
          }
      }
    }
    else
    {
        printf("Invalid Input");
    }
        
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
