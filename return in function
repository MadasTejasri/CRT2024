#include <stdio.h>
 int add(int s,int z)
    {
        int r=s+z;
        return r;
        
    }
     int main() {
    // Write C code here
    int a,b;
    scanf("%d %d",&a,&b);
      int k=add(a,b);
      printf("%d",k);
}
