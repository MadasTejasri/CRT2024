#include <stdio.h>
#include<string.h>
int main() {
    // Write C code here
    char str1[50],str2[50];
    int i;
    scanf("%s",str1);
    scanf("%s",str2);
    strcat(str1,str2);
    printf("%s",str1);
    return 0;
}
