#include <stdio.h>
int main() {
    // Write C code here
    int n;
    scanf("%d",&n);
    int a[n],i,j,t;
    for(i=0;i<n;i++)
    {
     scanf(" %d",&a[i]);
    }
    int s=0;
    for(i=0;i<n;i++)
    {
        s=s+a[i];
    printf(" %d",s);
    }
    return 0;
}
