#include <stdio.h>
#include<math.h>
int main() {
    // Write C code here
    float a,b,c;
    scanf("%f %f %f",&a,&b,&c);
    float d=b*b-4*a*c;
    if(d<0)
    {
        printf("Roots are not possible");
    }
    else
    {
    float r1=(-b+sqrt(d))/2*a;
    float r2=(-b-sqrt(d))/2*a;
     printf("%f\n",r1);
        printf("%f",r2);
    }
    return 0;
}
