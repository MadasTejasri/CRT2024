#include <stdio.h>
#include<math.h>
void reverse(int n)
{
    int d,r=0;
    while(n>0)
    {
    d=n%10;
    n=n/10;
    r=r*10+d;
    }
    printf("%d",r);
}
int main() {
    // Write C code here
    int n,r,d;
    scanf("%d",&n);
    reverse(n);
}
