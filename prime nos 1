#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b,i;
    scanf("%d\n%d",&a,&b);
    i=a;
    while(i<=b)
    {
        int c=0;
    for(int j=2;j<25;j++)
    {
        if(i%j==0)
        {
           c=c+1;
        }
    }
        if(c==0)
        {
         printf("%d ",i);
        }
        i++;
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
