// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    int n,r=1;
    scanf("%d",&n);
    while(n>=1)
    {
        r=r*n;
        n=n-1;
    }
        printf("%d",r);
    return 0;
}
